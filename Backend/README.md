# Backend API

Node.js and Express.js backend for OTP Authentication System.

## Features

* User Registration
* Login Authentication
* Email Verification using OTP
* JWT Authentication
* MongoDB Integration
* Nodemailer Email Service

## Setup

Install dependencies:

```bash
npm install
```

Create a .env file based on .env.example

Start the server:

```bash
npm run dev
```